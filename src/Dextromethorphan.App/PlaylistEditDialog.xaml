<Window x:Class="Dextromethorphan.App.PlaylistEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Playlist" Width="560" Height="620" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}">
    <Border Padding="24" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock x:Name="Heading" Text="New playlist" FontSize="22" FontWeight="SemiBold"/>
            <StackPanel Grid.Row="1" Margin="0,18,0,0"><TextBlock Text="Name" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/><TextBox x:Name="NameBox" Margin="0,5,0,0"/></StackPanel>
            <StackPanel Grid.Row="2" Margin="0,12,0,0"><TextBlock Text="Description" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/><TextBox x:Name="DescriptionBox" Margin="0,5,0,0" Height="56" TextWrapping="Wrap" AcceptsReturn="True"/><StackPanel Orientation="Horizontal" Margin="0,7,0,0"><Button Content="Choose cover" Click="ChooseCover_Click" Padding="10,5"/><TextBlock x:Name="CoverStatus" Text="No custom cover" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" VerticalAlignment="Center" Margin="8,0,0,0"/></StackPanel></StackPanel>
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,14,0,0"><RadioButton x:Name="ManualRadio" Content="Manual playlist" IsChecked="True" Checked="Kind_Changed" Margin="0,0,18,0"/><RadioButton x:Name="SmartRadio" Content="Smart playlist" Checked="Kind_Changed"/></StackPanel>
            <ScrollViewer Grid.Row="4" Margin="0,14,0,14" VerticalScrollBarVisibility="Auto"><StackPanel x:Name="SmartPanel" Visibility="Collapsed">
                <StackPanel Orientation="Horizontal"><TextBlock Text="Match" VerticalAlignment="Center" Margin="0,0,8,0"/><ComboBox x:Name="MatchBox" Width="90" ItemsSource="{Binding MatchOptions}"/></StackPanel>
                <TextBlock Text="Rules" FontWeight="SemiBold" Margin="0,14,0,6"/>
                <StackPanel x:Name="ConditionsHost"/>
                <Button Content="Add rule" Click="AddRule_Click" HorizontalAlignment="Left" Padding="12,6" Margin="0,10,0,0"/>
                <StackPanel Orientation="Horizontal" Margin="0,16,0,0"><TextBlock Text="Sort by" VerticalAlignment="Center" Margin="0,0,8,0"/><ComboBox x:Name="SortBox" Width="120" ItemsSource="{Binding SortOptions}"/><CheckBox x:Name="DescendingCheck" Content="Descending" Margin="16,0,0,0" VerticalAlignment="Center"/></StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0"><TextBlock Text="Limit" VerticalAlignment="Center" Margin="0,0,8,0"/><TextBox x:Name="LimitBox" Width="80" ToolTip="Optional maximum number of tracks"/><Button Content="Preview count" Click="Preview_Click" Padding="10,5" Margin="12,0,0,0"/><TextBlock x:Name="PreviewText" Foreground="{DynamicResource AccentBrush}" FontSize="10" VerticalAlignment="Center" Margin="10,0,0,0" TextWrapping="Wrap"/></StackPanel>
                <TextBlock Text="Nested groups (optional JSON)" FontWeight="SemiBold" Margin="0,16,0,5"/><TextBox x:Name="NestedRulesBox" Height="92" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" ToolTip="Paste a SmartRuleGroup JSON object to combine nested AND/OR groups."/>
            </StackPanel></ScrollViewer>
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="Cancel" Click="Cancel_Click" Padding="16,8" Margin="0,0,8,0"/><Button Content="Save" Style="{StaticResource PrimaryButton}" Click="Save_Click" Padding="18,8"/></StackPanel>
        </Grid>
    </Border>
</Window>
