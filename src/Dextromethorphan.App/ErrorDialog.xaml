<Window x:Class="Dextromethorphan.App.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Dextromethorphan encountered a problem"
        Width="640"
        SizeToContent="Height"
        MaxHeight="720"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource AppBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="46" ResizeBorderThickness="0" GlassFrameThickness="0" CornerRadius="12"/>
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Dextromethorphan encountered a problem" FontSize="20" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ContextText}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource IconButton}" Content="&#xE711;" Click="Exit_Click" ToolTip="Close"/>
            </Grid>
            <TextBlock Grid.Row="1" Text="{Binding MessageText}" TextWrapping="Wrap" FontSize="14" Margin="0,22,0,0"/>
            <Expander Grid.Row="2" Header="Technical details" Margin="0,18,0,0">
                <Border Background="{StaticResource SurfaceRaisedBrush}" CornerRadius="8" Padding="12" Margin="0,8,0,0" MaxHeight="320">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBox Text="{Binding DetailsText}" IsReadOnly="True" TextWrapping="Wrap" BorderThickness="0" Background="Transparent" FontFamily="Cascadia Mono" FontSize="10"/>
                    </ScrollViewer>
                </Border>
            </Expander>
            <Grid Grid.Row="3" Margin="0,22,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="Copy details" Click="Copy_Click" Margin="0,0,8,0"/>
                <Button Grid.Column="1" Content="Open log folder" Click="OpenLogs_Click"/>
                <Button Grid.Column="3" Content="Continue" Click="Continue_Click" Visibility="{Binding ContinueVisibility}" Margin="8,0,0,0"/>
                <Button Grid.Column="4" Content="Restart" Click="Restart_Click" Margin="8,0,0,0"/>
                <Button Grid.Column="5" Content="Exit" Click="Exit_Click" Margin="8,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
